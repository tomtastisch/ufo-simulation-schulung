[texts.setup_header]
license = """
UFO-Simulation Schulung – Lizenzhinweise (Kurzfassung)

Dieses Schulungsprojekt basiert auf der im Repository angegebenen Lizenz.
Die Nutzung erfolgt im Rahmen von Ausbildung, Studium und interner Schulung.
"""
title = "──────────────────────────── UFO-Simulation – Setup ────────────────────────────"
intro = "Willkommen zum automatisierten Setup der UFO-Simulation."
body = """
Dieses Programm richtet eine isolierte Python-Umgebung ein,
installiert alle benötigten Abhängigkeiten und prüft die Installation.

In diesem Setup werden nacheinander ausgeführt:

1. Prüfung der Python-Version
2. Erzeugen bzw. Wiederverwenden der virtuellen Umgebung (.venv)
3. Installation der Runtime-Dependencies
4. Installation der Dev-Dependencies
5. Import-Contract-Prüfung via import-linter
6. Ausführen der pytest-Tests zur Validierung der Installation

Dies kann einen Moment dauern – bitte warte, bis alle Schritte abgeschlossen sind.
"""
[texts.module_setup]
default = "Setup-{typ} {module}: {info}"

[texts.tests]
skipped_hint = "pytest wurde aufgrund der Skip-Konfiguration nicht ausgeführt."

[texts.args]
default = "{type} wurde aufgrund des arguments {arg} {action}"

[texts.next]
title = "Nächste Schritte"
body = """
✅ Setup erfolgreich abgeschlossen!

Nächste Schritte:

1. 📂 Virtuelle Umgebung aktivieren:
   {activate_cmd}

2. 🧪 Tests ausführen:
   pytest

3. 🎓 Aufgaben bearbeiten:
   - Aufgabe 1: Winkelberechnung (src/task/angle/angle.py)
   - Aufgabe 2: UFO-Autopilot (src/task/autopilot/autopilot.py)

4. 🚀 Simulation starten:
   python -m core.simulation

5. 📚 Dokumentation lesen:
   - README.md
   - docs/description/tasks/

Bei Fragen: siehe README.md oder Repository Issues
"""

[texts.legend]
body = """
Legende der Symbole:

{info}    – Information / Hinweis
{success} – Schritt erfolgreich abgeschlossen
{warning} – Warnung (Setup kann weiterlaufen, Ergebnis prüfen)
{error}   – Fehler (Setup wird abgebrochen)
{continue} – Hinweise zu Start und nächsten Schritten
"""

[icons]
info = "ℹ️"
success = "✅"
warning = "⚠️"
error = "❌"
continue = "🚀"
start = "🔄"
run = "⏳"
ok = "✅"
fail = "❌"

[texts.step_default]
success = "{step}: erfolgreich abgeschlossen."
failure = "{step}: Fehler unter {env}: {cause}\n{details}"
default = "{step}: {details}"

# Neu: generische Progress-Zeile für laufende Arbeiten
progress_running = "Running   /   {details}"
progress_failed = "Failed     /   {details}"
progress_finished = "Finished /   {details}"

# Neu: Standard-Details für Installationsfortschritt (kann von anderen Steps wiederverwendet werden)
install_details = "Installiere: {package} ({index}/{total})"
install_done = "Alle Abhängigkeiten installiert."
install_failed = "{package}: {details}"

[texts.RunTestsStep]
failure = "Tests im Schritt {step} unter {env} fehlgeschlagen: {cause}\n{details}"
tests_done = "Alle Tests erfolgreich."
tests_failed = "Tests fehlgeschlagen – Details siehe Log."
running_init = "Tests starten ({total_tests} Tests in {total_files} Dateien)"
running_progress = "{icon} {file_name} ({completed}/{total})"
finished_success = "Alle Tests erfolgreich ({total_tests} Tests in {total_files} Dateien)"
failed_detail = "{file_name}: {cause}"

[texts.help]
title = "UFO-Simulation Setup – Hilfe"
usage = """
Verwendung:
  python setup_v2.py [OPTIONEN] [BEFEHLE]

Beispiele:
  python setup_v2.py              # Standard-Setup (alle Steps)
  python setup_v2.py --help       # Diese Hilfe anzeigen
  python setup_v2.py --steps create_env,dependencies  # Nur bestimmte Steps
  python setup_v2.py --skip tests # Alle Steps außer Tests
  python setup_v2.py --verbose    # Ausführliche Ausgabe

Konfiguration:
  Setup-Verhalten wird über [tool.setup] in pyproject.toml gesteuert.
  Siehe Dokumentation für Details.

Verfügbare Steps:
{steps_list}
"""

[texts.typing_check]
header = "Typannotationen prüfen"
success = "Alle Dateien in src/task/* haben vollständige Typannotationen"
failure = "Typannotationen-Check fehlgeschlagen: {cause}"
warning = "Fehlende Annotationen in {file}"
missing_param = "  Zeile {line}: Parameter '{name}' ohne Typannotation"
missing_return = "  Zeile {line}: Funktion '{name}' ohne Return-Typannotation"
missing_variable = "  Zeile {line}: Variable '{name}' ohne Typannotation"
hint = """
💡 Tipp: Typannotationen helfen beim Lernen und vermeiden Fehler!

Beispiel:
  def calculate(x: float, y: float) -> float:
      return x + y
"""
no_files = "Verzeichnis src/task/ nicht gefunden (übersprungen)"