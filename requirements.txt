# Runtime-Dependencies für die UFO-Simulation
# HINWEIS: Die primären Dependencies sind jetzt in pyproject.toml definiert.
# Diese Datei wird für Kompatibilität mit älteren Tools beibehalten.

# Production Dependencies (siehe auch pyproject.toml [project.dependencies])
PyQt5>=5.15.11
PyQt5-sip>=12.17.1
numpy>=2.3.5

# Testing & Development Tools (siehe auch pyproject.toml [project.optional-dependencies.dev])
pytest>=8.0.0
pytest-timeout>=2.3.1
threadpoolctl>=3.5.0
py-spy>=0.3.14